import React from 'react';
import Sha256sum from '../components/Sha256sum';

const Comment = () => {
    return (
        <div>
            <h1>Comment page</h1>
            <Sha256sum />
        </div>
    );
};

export default Comment;